const welcome = (number, groupname) => {
    return `Jode @${number}. Que bueno verte por aqui🥺 ${groupname} lee las reglas para no tener mal entendidos crack 😉`
}
exports.welcome = welcome

const bye = (number) => {
    return `Que bueno que te fuiste😵 @${number}. 😈 ya no te queríamos ojala nunca regreses👋`
}
exports.bye = bye